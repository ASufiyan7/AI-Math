[build]
  command = "cd front && npm install && npm run build"
  publish = "front/dist"
  functions = "back"  publish = "front/dist"
  functions = "back"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/main/:splat"
  status = 200